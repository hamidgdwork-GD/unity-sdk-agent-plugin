# Antigravity Instructions

Read `AGENTS.md` first.

For Unity mobile notifications, use the full `gley-remote-config` workflow. The task is not complete until the Unity-side notification settings configurator has run or has been clearly reported as pending.

