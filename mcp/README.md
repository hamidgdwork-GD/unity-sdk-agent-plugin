# MCP Server

This folder is reserved for the future MCP server wrapper.

The planned MCP tools are:

- `list_unity_integrations`
- `scan_unity_project`
- `add_unity_integration`
- `validate_unity_integration`

For version 0.1, use the CLI in `cli/unity_sdk_agent.py`.

