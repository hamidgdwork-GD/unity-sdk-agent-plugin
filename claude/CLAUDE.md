# Unity SDK Agent Plugin

When asked to add Unity SDK integrations, use this repository as the integration toolkit.

Read `codex/SKILL.md` for the standard agent workflow. The instructions are intentionally agent-agnostic and should be followed by Claude Code as well.

Use `cli/unity_sdk_agent.py` for supported integrations and validate after changes.

